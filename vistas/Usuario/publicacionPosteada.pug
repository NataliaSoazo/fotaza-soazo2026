block content

  .container.d-flex.justify-content-center.align-items-center.vh-100

    .card.shadow.p-4(style="width: 400px;")

      if ok
        .alert.alert-success.alert-dismissible.fade.show.mt-3(role="alert")

          strong ✔ cargado correctamente

          button.btn-close(
            type="button"
            data-bs-dismiss="alert"
          )

      h3.text-center.mb-4 Nueva Publicación

      // PREVIEWS
      div#imgsPreview.d-flex.flex-wrap.mb-3
      h1 Imagen Resultante
    each imagen in imagenes
      p #{imagen.name}
      img(src=imagen.src)

  script(
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
  )