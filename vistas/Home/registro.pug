doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    title Registro
    meta(name="viewport" content="width=device-width, initial-scale=1")

    // Bootstrap 5
    link(
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    )

  body.bg-black
      .container.p-5.col-4
        .card.shadow.p-4(style="width: 400px;") 
            h3.text-center.mb-4 Crea tu cuenta en nuestra comunidad
            if mensaje
              .alert.alert-danger.mt-3
                = mensaje
            form(action="/registro" method="POST")
              .mb-3
                label.form-label Nombre
                input.form-control(
                  type="text"
                  name="nombre"
                  value= datos ? datos.nombre : ""
                  required
                )
              .mb-3
                label.form-label Apellido
                input.form-control(
                  type="text"
                  name="apellido"
                  value= datos ? datos.apellido : ""
                  required
                )
              // Email
              .mb-3
                label.form-label Nick que deseas usar
                input.form-control(
                  type="text"
                  name="nick"
                  value= datos ? datos.nick : ""
                  required
                )

              
              .mb-3
                label.form-label Teléfono
                input.form-control(
                  type="text"
                  name="telefono"
                  value= datos ? datos.telefono : ""
                  required
                )
              // Email
              .mb-3
                label.form-label  E-mail
                input.form-control(
                  type="email"
                  name="mail"
                  value= datos ? datos.mail : ""
                  required
                )
              // Contraseña
              .mb-3
                label.form-label Contraseña
                input.form-control(
                  type="password"
                  name="password"
                  required
                )
              // Términos
              .form-check.mb-3
                input.form-check-input(
                  type="checkbox"
                  required
                )
                label.form-check-label
                  | Acepto los términos y condiciones

              // Botón
              button.btn.btn-success.w-100(type="submit")
                | Registrarse

            p.text-center.mt-3.mb-0
              | ¿Ya tenés cuenta?
              a(href="/ingreso")  Iniciar sesión

        script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")